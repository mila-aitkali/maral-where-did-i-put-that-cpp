#pragma once

// Finds the first occurrence of target in s.
// Returns index if found, otherwise -1.
int find(const char* s, char target);

// Finds the first occurrence of sub in s.
// Returns starting index if found, otherwise -1.
int find(const char* s, const char* sub);